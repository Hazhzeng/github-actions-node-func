name: WIN_CON_NODE_SCM_CREDENTIALS

on:
  push:
    branches:
    - master

jobs:
  BuildAndDeploy:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@master
    - uses: Azure/functions-action@v1
      id: fa
      with:
        app-name: hazeng-fa-github-win-con-node-scm
        publish-profile: ${{ secrets.WIN_CON_NODE_SCM_CREDENTIALS }}
    - run: echo "The result is ${{ steps.fa.outputs.app-url }}"
    - name: 'Report HttpTrigger Result'
      shell: bash
      run: |
        i="0"
        while [ $i -lt 10 ]; do
          sleep 5
          RESPONSE=$(curl "${{ steps.fa.outputs.app-url }}/api/HttpTrigger")
          if [ $RESPONSE == IAUEFIQJFKASHDV ]; then
            exit 0
          fi
          i=$[$i+1]
        done
        exit 1
